{
  "configFile": "jedit-example.xml",
  "summaryCount": 25,
  "summaries": [
    {
      "methodId": 3905,
      "methodName": "getNextToken",
      "className": "ParserTokenManager",
      "summary": "This method gets the token and returns a Token. That Token is used by methods that gets the token and that gets the token. GetNextToken() seems far more important than average because it is called by 2 methods. The method can be used in an assignment statement ; for example: \"else t=t.next=token_source.getNextToken();\"."
    },
    {
      "methodId": 4441,
      "methodName": "event",
      "className": "JThis",
      "summary": "This method handles the event. Return data is used by some methods that handles the caret update. Return data is used by some methods that handles the vetoable change. Event() seems far more important than average because it is called by 10 methods. The method can be used in a procedural statement ; for example: \"event(\"vetoableChange\",e);\"."
    },
    {
      "methodId": 4580,
      "methodName": "readChar",
      "className": "JavaCharStream",
      "summary": "This method reads the char and returns a char. That char is used by methods that gets the token and that begins the token. ReadChar() seems far more important than average because it is called by 2 methods. The method can be used in a procedural statement ; for example: \"input_stream.readChar();\"."
    },
    {
      "methodId": 2124,
      "methodName": "getProperty",
      "className": "PropertyManager",
      "summary": "This method gets the property and returns a String. GetProperty() seems far more important than average because it is not called by any methods."
    },
    {
      "methodId": 4346,
      "methodName": "getSourceFile",
      "className": "SimpleNode",
      "summary": "This method gets the source file and returns a String. That String is used by methods that gets the source file and that converts the string. Return data is used by some methods that gets the source file. Return data is used by some methods that converts the string. GetSourceFile() seems far more important than average because it is called by 5 methods."
    },
    {
      "methodId": 4587,
      "methodName": "backup",
      "className": "JavaCharStream",
      "summary": "This method backups the int. Return data is used by a method that reads the char. Return data is used by a method that gets the token. Backup() seems far more important than average because it is called by 3 methods. The method can be used in a procedural statement ; for example: \"if (jjmatchedPos + 1 < curPos) input_stream.backup(curPos - jjmatchedPos - 1);\"."
    },
    {
      "methodId": 4583,
      "methodName": "getEndColumn",
      "className": "JavaCharStream",
      "summary": "This method gets the end column and returns an int. That int is used by methods that fills the token and that gets the token. GetEndColumn() seems far more important than average because it is called by 3 methods. The method can be used in an assignment statement ; for example: \"int error_column=input_stream.getEndColumn();\"."
    },
    {
      "methodId": 4584,
      "methodName": "getEndLine",
      "className": "JavaCharStream",
      "summary": "This method gets the end line and returns an int. That int is used by methods that gets the token and that fills the token. GetEndLine() seems far more important than average because it is called by 3 methods. The method can be used in an assignment statement ; for example: \"int error_line=input_stream.getEndLine();\"."
    },
    {
      "methodId": 4600,
      "methodName": "GetImage",
      "className": "JavaCharStream",
      "summary": "This method gets the image and returns a String. That String is used by methods that fills the token and that gets the token. GetImage() seems far more important than average because it is called by 2 methods. The method can be used in an assignment statement ; for example: \"error_after=curPos <= 1 ? \"\" : input_stream.GetImage();\"."
    },
    {
      "methodId": 5930,
      "methodName": "getLineStartOffset",
      "className": "JEditBuffer",
      "summary": "This method gets the line start offset and returns an int. That int is used by methods that gets the line length and that gets the line start offset. GetLineStartOffset() seems far more important than average because it is called by 10 methods. The method can be used in a procedural statement ; for example: \"return buffer.getLineStartOffset(line);\"."
    },
    {
      "methodId": 6272,
      "methodName": "getLineCount",
      "className": "LineManager",
      "summary": "This method gets the line count and returns an int. That int is used by methods that gets the line count and that gets the line start offset. GetLineCount() seems far more important than average because it is called by 8 methods. The method can be used in an assignment statement ; for example: \"if (line < 0 || line >= lineMgr.getLineCount()) throw new ArrayIndexOutOfBoundsException(line);\"."
    },
    {
      "methodId": 2129,
      "methodName": "getDefaultProperty",
      "className": "PropertyManager",
      "summary": "This method gets the default property and returns a String. That String is used by methods that gets the property and that sets the property. GetDefaultProperty() seems far more important than average because it is called by 3 methods. The method can be used in a procedural statement ; for example: \"else return getDefaultProperty(name);\"."
    },
    {
      "methodId": 4577,
      "methodName": "BeginToken",
      "className": "JavaCharStream",
      "summary": "This method begins the token and returns a char. That char is used by a method that gets the token. BeginToken() seems far more important than average because it is called by 1 method. The method can be used in an assignment statement ; for example: \"curChar=input_stream.BeginToken();\"."
    },
    {
      "methodId": 3904,
      "methodName": "jjFillToken",
      "className": "ParserTokenManager",
      "summary": "This method fills the token and returns a Token. That Token is used by some methods that gets the token. JjFillToken() seems far more important than average because it is called by 1 method. The method can be used in an assignment statement ; for example: \"matchedToken=jjFillToken();\"."
    },
    {
      "methodId": 2411,
      "methodName": "getInputHandler",
      "className": "View",
      "summary": "This method gets the input handler and returns an InputHandler. That InputHandler is used by methods that handles the pass key event to view and that gets the input handler. GetInputHandler() seems far more important than average because it is called by 5 methods."
    },
    {
      "methodId": 5274,
      "methodName": "getToken",
      "className": "Parser",
      "summary": "This method gets the token and returns a Token. That Token is used by methods that jjtrees the open node scope and that jjtrees the close node scope. GetToken() seems far more important than average because it is called by 5 methods. The method can be used in an assignment statement ; for example: \"((SimpleNode)n).lastToken=getToken(0);\"."
    },
    {
      "methodId": 3588,
      "methodName": "getNode",
      "className": "NameSpace",
      "summary": "This method gets the node and returns a SimpleNode. That SimpleNode is used by methods that gets the script stack trace and that gets the node. GetNode() seems far more important than average because it is called by 4 methods. The method can be used in an assignment statement ; for example: \"if (parent != null) return parent.getNode();\"."
    },
    {
      "methodId": 2125,
      "methodName": "setProperty",
      "className": "PropertyManager",
      "summary": "This method sets the property. SetProperty() seems far more important than average because it is not called by any methods."
    },
    {
      "methodId": 3615,
      "methodName": "getMethod",
      "className": "NameSpace",
      "summary": "This method gets the method and returns a BshMethod. That BshMethod is used by methods that gets the method and that handles the event. GetMethod() seems far more important than average because it is called by 9 methods. The method can be used in an assignment statement ; for example: \"method=namespace.getMethod(\"handleEvent\",new Class[]{null});\"."
    },
    {
      "methodId": 3616,
      "methodName": "getMethod",
      "className": "NameSpace",
      "summary": "This method gets the method and returns a BshMethod. GetMethod() seems far more important than average because it is not called by any methods."
    },
    {
      "methodId": 5928,
      "methodName": "getLineCount",
      "className": "JEditBuffer",
      "summary": "This method gets the line count and returns an int. That int is used by methods that gets the visible line int - and that expands the fold. GetLineCount() seems far more important than average because it is called by 10 methods. The method can be used in an assignment statement ; for example: \"if (line < 0 || line >= buffer.getLineCount()) throw new ArrayIndexOutOfBoundsException(line);\"."
    },
    {
      "methodId": 1618,
      "methodName": "getFlag",
      "className": "Buffer",
      "summary": "This method gets the flag int and returns a boolean. That boolean is used by methods that is the temporary and that saves the view. GetFlag() seems far more important than average because it is called by 10 methods. The method can be used in a procedural statement ; for example: \"return getFlag(TEMPORARY);\"."
    },
    {
      "methodId": 4557,
      "methodName": "invoke",
      "className": "BshMethod",
      "summary": "This method invokes the object and returns an Object. That Object is used by methods that invokes the object and that handles the event. Invoke() seems far more important than average because it is called by 8 methods. The method can be used in a procedural statement ; for example: \"method.invoke(new Object[]{event},declaringInterpreter,callstack,null);\"."
    },
    {
      "methodId": 4558,
      "methodName": "invoke",
      "className": "BshMethod",
      "summary": "This method invokes the object and returns an Object. Invoke() seems far more important than average because it is not called by any methods."
    },
    {
      "methodId": 4559,
      "methodName": "invoke",
      "className": "BshMethod",
      "summary": "This method invokes the object and returns an Object. Invoke() seems far more important than average because it is not called by any methods."
    }
  ]
}