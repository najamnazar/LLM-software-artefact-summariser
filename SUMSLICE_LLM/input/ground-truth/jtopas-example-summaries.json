{
  "configFile": "jtopas-example.xml",
  "summaryCount": 25,
  "summaries": [
    {
      "methodId": 453,
      "methodName": "setFlags",
      "className": "TokenizerProperty",
      "summary": "This method sets the flags. Return data is used by some methods that sets the flags. Return data is used by some methods that sets the flags. SetFlags() seems far more important than average because it is called by 2 methods."
    },
    {
      "methodId": 454,
      "methodName": "setFlags",
      "className": "TokenizerProperty",
      "summary": "This method sets the flags. SetFlags() seems far more important than average because it is not called by any methods."
    },
    {
      "methodId": 902,
      "methodName": "checkArgument",
      "className": "AbstractTokenizerProperties",
      "summary": "This method checks the argument. Return data is used by a method that gets the block comment. Return data is used by a method that gets the special sequence. CheckArgument() seems far more important than average because it is called by 7 methods. The method can be used in a procedural statement ; for example: \"checkArgument(pattern,\"Pattern\");\"."
    },
    {
      "methodId": 831,
      "methodName": "doGetProperty",
      "className": "AbstractTokenizerProperties",
      "summary": "This method does get the property and returns a TokenizerProperty. That TokenizerProperty is used by methods that gets the block comment and that gets the special sequence. DoGetProperty() seems far more important than average because it is called by 7 methods. The method can be used in a procedural statement ; for example: \"return doGetProperty(Token.PATTERN,pattern);\"."
    },
    {
      "methodId": 460,
      "methodName": "getImages",
      "className": "TokenizerProperty",
      "summary": "This method gets the images and returns a String[]. That String[] is used by methods that starts the sequence store (P) (NM) (NM) and that searches the EQUIV[string string. GetImages() seems far more important than average because it is called by 10 methods. The method can be used in an assignment statement ; for example: \"String img=prop.getImages()[0];\"."
    },
    {
      "methodId": 1058,
      "methodName": "searchString",
      "className": "SequenceStore",
      "summary": "This method searches the EQUIV[string string and returns a TokenizerProperty. That TokenizerProperty is used by methods that removes the special sequence and that gets the special sequence. SearchString() seems far more important than average because it is called by 2 methods. The method can be used in a procedural statement ; for example: \"return searchString(image,false);\"."
    },
    {
      "methodId": 733,
      "methodName": "getTokenizerProperties",
      "className": "AbstractTokenizer",
      "summary": "This method gets the tokenizer properties and returns a TokenizerProperties. That TokenizerProperties is used by methods that is the flag set and that is the flag set. GetTokenizerProperties() seems far more important than average because it is called by 5 methods. The method can be used in an assignment statement ; for example: \"return prop.isFlagSet(flag,(getTokenizerProperties().getParseFlags() & flag) != 0 || isFlagSet(flag));\"."
    },
    {
      "methodId": 772,
      "methodName": "getBaseTokenizer",
      "className": "AbstractTokenizer",
      "summary": "This method gets the base tokenizer and returns an AbstractTokenizer. That AbstractTokenizer is used by methods that reads the data and that gets the base data provider. GetBaseTokenizer() seems far more important than average because it is called by 4 methods. The method can be used in a procedural statement ; for example: \"return getBaseTokenizer().getDataProvider(startPos,length);\"."
    },
    {
      "methodId": 462,
      "methodName": "getCompanion",
      "className": "TokenizerProperty",
      "summary": "This method gets the companion and returns an Object. That Object is used by methods that equals the object and that gets the token. GetCompanion() seems far more important than average because it is called by 9 methods. The method can be used in a procedural statement ; for example: \"if (getType() == thatProp.getType() && getCompanion() == thatProp.getCompanion() && getFlags() == thatProp.getFlags()) {\"."
    },
    {
      "methodId": 773,
      "methodName": "getBaseDataProvider",
      "className": "AbstractTokenizer",
      "summary": "This method gets the base data provider and returns a DataProvider. That DataProvider is used by methods that gets the char and that reads the whitespaces. GetBaseDataProvider() seems far more important than average because it is called by 6 methods. The method can be used in a procedural statement ; for example: \"return getBaseDataProvider(pos,1).getCharAt(0);\"."
    },
    {
      "methodId": 774,
      "methodName": "readMoreDataFromBase",
      "className": "AbstractTokenizer",
      "summary": "This method reads the data and returns an int. That int is used by methods that is the whitespace and that is the whitespace. ReadMoreDataFromBase() seems far more important than average because it is called by 10 methods. The method can be used in an assignment statement ; for example: \"if (_currentReadPos + offset >= _currentWritePos && readMoreDataFromBase() < 0) {\"."
    },
    {
      "methodId": 893,
      "methodName": "removeProperty",
      "className": "AbstractTokenizerProperties",
      "summary": "This method removes the EQUIV[property tokenizer property. Return data is used by some methods that removes the EQUIV[keyword string. Return data is used by some methods that removes the EQUIV[line comment string - (NM). RemoveProperty() seems far more important than average because it is called by 6 methods. The method can be used in a procedural statement ; for example: \"removeProperty(prop);\"."
    },
    {
      "methodId": 1160,
      "methodName": "getCharAt",
      "className": "DataProvider",
      "summary": "This method gets the char int - and returns a char. That char is used by methods that starts the sequence store (P) (NM) (NM) and that gets the char. GetCharAt() seems far more important than average because it is called by 5 methods. The method can be used in an assignment statement ; for example: \"char startChar=getStartChar(dataProvider.getCharAt(0));\"."
    },
    {
      "methodId": 1062,
      "methodName": "compare",
      "className": "SequenceStore",
      "summary": "This method compares the string and returns an int. That int is used by methods that searches the EQUIV[string string and that compares the string. Compare() seems far more important than average because it is called by 5 methods. The method can be used in an assignment statement ; for example: \"int res=compare(img,image,1);\"."
    },
    {
      "methodId": 1064,
      "methodName": "compare",
      "className": "SequenceStore",
      "summary": "This method compares the char and returns an int. Compare() seems far more important than average because it is not called by any methods."
    },
    {
      "methodId": 789,
      "methodName": "isFlagSet",
      "className": "AbstractTokenizer",
      "summary": "This method is the flag set and returns a boolean. That boolean is used by methods that is the flag set and that is the flag set. IsFlagSet() seems far more important than average because it is called by 10 methods. The method can be used in an assignment statement ; for example: \"return prop.isFlagSet(flag,(getTokenizerProperties().getParseFlags() & flag) != 0 || isFlagSet(flag));\"."
    },
    {
      "methodId": 790,
      "methodName": "isFlagSet",
      "className": "AbstractTokenizer",
      "summary": "This method is the flag set and returns a boolean. IsFlagSet() seems far more important than average because it is not called by any methods."
    },
    {
      "methodId": 452,
      "methodName": "getType",
      "className": "TokenizerProperty",
      "summary": "This method gets the type and returns an int. That int is used by methods that adds the EQUIV[property tokenizer property and that equals the object. GetType() seems far more important than average because it is called by 8 methods. The method can be used in a procedural statement ; for example: \"switch (property.getType()) {\"."
    },
    {
      "methodId": 1044,
      "methodName": "startsWithSequenceCommentOrString",
      "className": "SequenceStore",
      "summary": "This method starts the sequence store (P) (NM) (NM) and returns a TokenizerProperty. That TokenizerProperty is used by methods that starts the standard tokenizer properties (P) (NM) (NM) and that is the keyword. StartsWithSequenceCommentOrString() seems far more important than average because it is called by 2 methods. The method can be used in a procedural statement ; for example: \"return startsWithSequenceCommentOrString(dataProvider);\"."
    },
    {
      "methodId": 456,
      "methodName": "getFlagMask",
      "className": "TokenizerProperty",
      "summary": "This method gets the flag mask and returns an int. That int is used by some methods that contains the EQUIV[flag int. GetFlagMask() seems far more important than average because it is called by 1 method. The method can be used in a procedural statement ; for example: \"return (getFlagMask() & flag) == flag;\"."
    },
    {
      "methodId": 867,
      "methodName": "getBlockComment",
      "className": "AbstractTokenizerProperties",
      "summary": "This method gets the block comment and returns a TokenizerProperty. That TokenizerProperty is used by methods that blocks the comment exists and that gets the block comment companion. GetBlockComment() seems far more important than average because it is called by 3 methods. The method can be used in an assignment statement ; for example: \"return getBlockComment(start) != null;\"."
    },
    {
      "methodId": 875,
      "methodName": "getSpecialSequence",
      "className": "AbstractTokenizerProperties",
      "summary": "This method gets the special sequence and returns a TokenizerProperty. That TokenizerProperty is used by methods that gets the special sequence companion and that gets the special sequence exists. GetSpecialSequence() seems far more important than average because it is called by 3 methods. The method can be used in an assignment statement ; for example: \"return getSpecialSequence(specSeq) != null;\"."
    },
    {
      "methodId": 883,
      "methodName": "getKeyword",
      "className": "AbstractTokenizerProperties",
      "summary": "This method gets the EQUIV[keyword string and returns a TokenizerProperty. That TokenizerProperty is used by methods that gets the keyword exists and that removes the EQUIV[keyword string. GetKeyword() seems far more important than average because it is called by 3 methods. The method can be used in an assignment statement ; for example: \"return getKeyword(keyword) != null;\"."
    },
    {
      "methodId": 891,
      "methodName": "getPattern",
      "className": "AbstractTokenizerProperties",
      "summary": "This method gets the EQUIV[pattern string - and returns a TokenizerProperty. That TokenizerProperty is used by methods that gets the pattern companion and that patterns the exists. GetPattern() seems far more important than average because it is called by 3 methods. The method can be used in an assignment statement ; for example: \"return getPattern(pattern) != null;\"."
    },
    {
      "methodId": 851,
      "methodName": "getString",
      "className": "AbstractTokenizerProperties",
      "summary": "This method gets the EQUIV[string string and returns a TokenizerProperty. That TokenizerProperty is used by methods that gets the string companion and that removes the EQUIV[string string. GetString() seems far more important than average because it is called by 3 methods. The method can be used in an assignment statement ; for example: \"return getString(start) != null;\"."
    }
  ]
}